# AutoPatch — SonarQube / SonarCloud project properties
# These are used by the sonar-scanner CLI when scanning THIS repo.
# For scanning OTHER repos, the CLI args in the workflow take precedence.

sonar.projectKey=test-project
# sonar.organization=your-org-key  # Uncomment and set for SonarCloud